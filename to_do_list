------------------------------CRASH----------------------------------
(exit dans les pipes doit pas quitter)
(gerer overflow de exit)

/!\ # supprimer cmd_lst_print du push final

--------------------------------------------------------------------------------
(export $ARG="echo salut" n'exec pas la commande echo pour afficher salut) -> ? (marche pas dans mon bash)
--------------------------------------------------------------------------------

